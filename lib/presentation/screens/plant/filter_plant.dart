import 'package:flutter/material.dart';

class FilterPlant extends StatefulWidget {
  const FilterPlant({Key? key}) : super(key: key);

  @override
  State<FilterPlant> createState() => _FilterPlantState();
}

class _FilterPlantState extends State<FilterPlant> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
